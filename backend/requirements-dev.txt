-r requirements.txt
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
xlwt>=1.3.0           # writes .xls fixtures for the bill-status-parser tests
moto[s3]>=5.0         # in-memory S3 for the storage tests (no AWS account needed)
